function make_shirt(size, text){
    console.log(`The size of the shirt is ${size} and the text written over it is ${text}`);
}

let size:  number = 5;
let text: string = "WOW"
make_shirt(size, text);
