// let console.log()
var obj: {type: string, car:string, model:number} = {
    type: "Sedan",
    car: "Mercedes",
    model: 2004
};


// accessing initial values of the object
console.log("Object type = " + obj.type + "\ncar = " + obj.car + "\nmodel = " + obj.model);